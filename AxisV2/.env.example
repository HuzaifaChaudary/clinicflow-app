# ──────────────────────────────────────────────
# AxisV2 Frontend — Environment Variables
# Copy this file to .env and adjust as needed
# ──────────────────────────────────────────────

# Toggle: "true" for production, "false" for local dev
VITE_PRODUCTION=false

# Main backend API (Vercel serverless — waitlist submissions)
# Leave blank to use defaults: localhost:8000 (dev) or Vercel URL (prod)
VITE_API_URL=

# Ava voice/SMS server (DigitalOcean droplet)
# Leave blank to use defaults: localhost:8002 (dev) or ava.useaxis.app (prod)
VITE_AVA_SERVER_URL=
