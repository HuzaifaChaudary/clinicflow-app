# ──────────────────────────────────────────────
# Ava AI Server — Environment Variables
# Copy this file to .env and fill in your keys
# ──────────────────────────────────────────────

# ── Retell AI (Orchestration Brain) ──────────
# Sign up at https://retellai.com, get API key from Dashboard → Settings
RETELL_API_KEY=key_edb760e24db873218e736645c12b

# Your public server URL (where Retell sends webhooks)
# Local dev: use ngrok (e.g. https://abc123.ngrok.io)
# Production: your deployed server URL
RETELL_WEBHOOK_BASE_URL=https://ava.useaxis.app/api/retell/webhook

# ElevenLabs voice ID (available through Retell — no separate API key needed)
# American voices: 11labs-Marissa, 11labs-Lily, 11labs-Kate, 11labs-Jenny, 11labs-Julia
# Browse all voices: https://dashboard.retellai.com/voices
RETELL_VOICE_ID=11labs-Marissa

# ElevenLabs API key (optional — only needed if using custom cloned voices)
# Get from https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# After running setup_retell_agent.py, paste the generated IDs here:
RETELL_AGENT_ID=
RETELL_LLM_ID=

# Set to "true" in production to verify webhook signatures
VERIFY_RETELL_WEBHOOK=false

# ── Twilio (Phone Infrastructure) ───────────
# Get these from https://console.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token-here

# Twilio phone number for voice (Call Ava)
TWILIO_VOICE_NUMBER=+15555550123

# Twilio phone number for SMS (Text Ava — still uses OpenAI)
TWILIO_SMS_NUMBER=+15555550124

# Twilio Elastic SIP Trunk — for routing calls to Retell
# Create at: https://console.twilio.com/us1/develop/sip-trunking/trunks
# See RETELL_SETUP.md for detailed instructions
TWILIO_SIP_TERMINATION_URI=your-trunk-name.pstn.twilio.com

# SIP trunk credentials (if using credential auth instead of IP whitelisting)
# If using IP whitelisting (add Retell IP 100.20.5.228), leave these empty
TWILIO_SIP_AUTH_USERNAME=
TWILIO_SIP_AUTH_PASSWORD=

# ── OpenAI (SMS only — voice now uses Retell) ─
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ── SMTP (for booking confirmation emails) ───
# Gmail: use an App Password (https://myaccount.google.com/apppasswords)
SMTP_EMAIL=sales@useaxis.app
SMTP_PASSWORD=your-app-password-here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# ── Google Sheets (waitlist data storage) ─────
GOOGLE_SHEETS_SPREADSHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_PROJECT_ID=

# ── Cal.com (Booking & Scheduling) ────────────
# Public booking page URL
CAL_BOOKING_URL=https://cal.com/axis-founders/15min
# Cal.com API key — get from https://cal.com/settings/developer/api-keys
CAL_API_KEY=cal_live_xxxxxxxxxxxx
# Event type ID — find via: curl -H "Authorization: Bearer CAL_API_KEY" https://api.cal.com/v1/event-types?apiKey=YOUR_KEY
CAL_EVENT_TYPE_ID=

# ── Waitlist API (legacy, optional) ──────────
WAITLIST_API_URL=http://localhost:8000/api/waitlist
